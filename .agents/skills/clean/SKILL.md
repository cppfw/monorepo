---
name: clean
description: Clean the CMake build tree using CMake's clean target. Use when you need to remove build artifacts.
---

# Clean (CMake) — Skill

Use CMake to clean the build tree.

Run:

`cmake --build out/<preset> --target clean`
