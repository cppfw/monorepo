# Run tests (CMake) — Skill

Run the CMake `test` target.

- Preferred: use the VS Code `Build_CMakeTools` tool with `buildTargets = ["test"]`.
- Fallback: run `cmake --build <build-directory> --target test`.
